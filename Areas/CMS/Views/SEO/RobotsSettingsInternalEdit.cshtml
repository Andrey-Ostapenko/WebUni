@{ Layout = null;}
@using livemenu.Helpers
@model livemenu.Common.Kernel.Settings.RobotsSettings


@using (Ajax.BeginForm(MVC.CMS.SEO.SaveRobotsSettings(), new AjaxOptions()
{
    HttpMethod = "POST",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "robotssettings-edit"
}))
{
    <div class="row">

        <div class="form-group">
            <label>Включить генерацию robots.txt </label>

            @Html.BasicCheckBoxFor(m => m.IsEnabled, new {data_init_plugin = "switchery"})
        </div>
        @*<div class="form-group">
            <label>Содержимое robots.txt </label>

            @Html.TextAreaFor(m => m.Content, new { @class = "form-control", rows=40 })
        </div>*@
    </div>
}
<script>
    $(document).ready(function() {
        //var editor = CKEDITOR.replaceAll('ckeditor');

        //for (var i in CKEDITOR.instances) {
        //    CKEDITOR.instances[i].on('change', function () { CKEDITOR.instances[i].updateElement(); });
        //};
        $.Pages.initSwitcheryPlugin('switchery');
    });
</script>
  